Installation
============

Requirements
------------

- Python >= 3.10
- Dependencies: numpy, MDAnalysis, lmfit, scipy, freud-analysis, pandas, requests, tqdm, tidynamics

Install
-------

From the repository root:

.. code-block:: bash

   pip install .

For development (editable install):

.. code-block:: bash

   pip install -e .
